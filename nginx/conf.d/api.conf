server {
    listen 80;
    server_name api.appilico.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Uncomment the block below AFTER running certbot successfully:
#
# server {
#     listen 443 ssl http2;
#     server_name api.appilico.com;
#
#     ssl_certificate /etc/letsencrypt/live/api.appilico.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.appilico.com/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     gzip on;
#     gzip_types text/plain application/json application/javascript text/css;
#
#     location / {
#         proxy_pass http://backend:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#         proxy_buffering off;
#         proxy_read_timeout 90s;
#     }
#
#     client_max_body_size 10M;
# }
